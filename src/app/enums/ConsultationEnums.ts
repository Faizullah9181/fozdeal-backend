// status of the consultation active , inactive, rejected, pending
export enum CONSULTATION_STATUS {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    REJECTED = 'rejected',
    PENDING = 'pending'
}

// slots of the consultation 30,60,90,120
export enum SLOTS {
    THIRTY = 30,
    SIXTY = 60,
    NINETY = 90,
    ONE_TWENTY = 120
}
